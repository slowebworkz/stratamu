{
  "exports": {
    ".": {
      "import": "./dist/telnet.js",
      "require": "./dist/telnet.js"
    }
  },
  "name": "@stratamu/networking",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@stratamu/types": "workspace:*"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:esm": "tsc -p tsconfig.esm.json",
    "build": "pnpm run clean && pnpm run build:esm",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "test": "tsc --noEmit -p tsconfig.vitest.json && vitest --watch"
  }
}
